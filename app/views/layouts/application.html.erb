<!DOCTYPE html>
<html>
  <head>
    <title>Lezzon</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', rel: 'stylesheet', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class=" h-100 d-flex justify-content-center align-items-center">
      <div>
        <% if flash[:error] -%>
          <div class="alert alert-danger" role="alert"><%=h flash[:error] %></div>
        <% end -%>
        <% if flash[:warning] -%>
          <div class="alert alert-warning" role="alert"><%=h flash[:warning] %></div>
        <% end -%>
        <% if flash[:notice] -%>
          <div class="alert alert-info" role="alert"><%=h flash[:notice] %></div>
        <% end -%>
        <%= yield %>
      </div>
    </div>
  </body>
</html>q
